{
  "name": "@capytale/contract-builder",
  "version": "1.0.0",
  "description": "Une librairie de types utiles pour déclarer une collection de contrats",
  "author": "Capytale.fr",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "build": "tsc && vite build",
    "prepublishOnly": "npm run build",
    "clean": "rm -rf lib/"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.11",
    "vite-plugin-dts": "^4.5.0"
  }
}
