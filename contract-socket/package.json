{
  "name": "@capytale/contract-socket",
  "version": "1.1.6",
  "description": "Ce module implémente le \"socket\" qui permet à une partie de brancher ses propres implémentations de contrats et aussi de consommer ceux de l'autre partie.",
  "author": "Capytale.fr",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "build": "tsc && vite build",
    "prepublishOnly": "npm run build",
    "clean": "rm -rf lib/",
    "test": "vitest"
  },
  "devDependencies": {
    "@capytale/contract-builder": "^1.0.0",
    "typescript": "^5.9.3",
    "vite": "^6.3.6",
    "vite-plugin-dts": "^4.5.4",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@capytale/contract-type": "^1.0.0"
  }
}
